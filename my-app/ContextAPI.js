/*

function App() {

  return (
    <div style={{backgroundColor: "red", padding:"20px"}}>
      <h1>App Component</h1>
      <UserProvider>
        <Parent />
      </UserProvider>
    </div>
  )
}

ContextAPI - It is an state Management Tool which is used in react. context api use three components in react js.

1). createContext() - initialize context (state ko initialize krta hai)
2). provider - provides and updates data (ek component se dusre component mai jab bhi hum data pass karate hai to uske liye provider ka use krte hai yhh data ko update bhi krta hai)
3). useContext() - Access context data


*/